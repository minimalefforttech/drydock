# Webview Visual Tests

The visual check matrix for the browser harness in this directory. Checks run
against the REAL bundled webviews (`dist/webview/*.js`) in a browser, a mock
host speaking the exact envelope protocol, and deterministic dummy fixtures
(no docker, no providers, no real folders). These checks validate UI wiring
and visuals; they cannot validate anything behind the host boundary (real
mounts, runtimes, toasts, the OS diff editor - those need guided manual
verification against a real VS Code window).

Run: `npm run bundle` (apps/vscode-extension) → start the `webview-harness`
launch config (port 8971) → drive with the preview tools. Scenario helpers on
`window.__harness.scenario`: `streamTurn(sessionId)`, `attention(sessionId,
reasons)`, `accessRequest(summary)`, `memoryCandidate(candidate)`,
`boardChanged()`, `plannerChanged(planId)`, `agentsTick()`,
`agentsTurnCompleted(status)`, `agentsChanged()`. Panel pages: `index.html`
(chat rail), `taskReview.html` (task review), `codeReview.html` (code
review), `taskBoard.html` (task board), `planner.html` (planner),
`agents.html` (agents fleet), `configure.html` (configure panel).

The Control Panel retired in ADR 0020; `index.html` now loads the chat rail
bundle, so every chat-side row below runs unchanged against it. Rows that
checked the Tasks / Plan / System tabs are marked RETIRED or PARTIALLY RETIRED
with their successor named. **Gap:** the left rail (`rail.js`) and the Task
Hub (`taskHub.js`) have no harness pages yet, so their successor behaviour
needs guided manual verification in a real window.

Fixture cast: sessions `s-live` (active, codex, has plan-less transcript with
markdown+mermaid), `s-waiting` (active, claude), `s-ended` (resume
target), `s-failed`; tasks `t-1` (linked set-1 + s-live, lastWorkedAt, 3
subtasks `st-1`/`st-2`/`st-3`), `t-2` (bare, 1 subtask `st-4`), `t-3` (task
board cast: linked set-1, sits in `col-review` with a recent `doneAt`, 2
subtasks - `st-5` in progress with a prompt, `st-6` in `col-finished` with
`doneAt` ~2 days old so the default 1-day age filter hides it); 6 seeded board
columns `col-backlog`/`col-todo`/`col-blocked`/`col-in-progress`/`col-review`/`col-finished`
(categories backlog/pending/pending/in-progress/done/done); of the 6 subtasks,
`st-2` is the only `isBlocked: true` and the only `autoStart: true`; pending
access requests `ar-rw` (read-write) and `ar-sens`
(read-only but `.env`, sensitive) + approved `ar-ok`; working set with
+12/−3, +40/−0, and an unrevertable delete.

| # | Check | How |
|---|---|---|
| V1 | RETIRED (ADR 0020): the four-tab strip is gone. `index.html` now hosts the chat rail (`chatRail.js`), which has no tabs - there is nothing to check on load beyond the chat itself (V6-V14) | - |
| V2 | RETIRED (ADR 0020): the Tasks tab's chat rows moved to the left rail's Recents view (`drydock.recents`) and the Agents panel's flat rows (V59). No harness page exists for the rail yet - guided manual verification | - |
| V3 | RETIRED (ADR 0020): task rows with column pill / worked-age / link chips moved to the left rail's Tasks view and the Task Hub; the column pill itself is checked on the board (V35, V62) | - |
| V4 | RETIRED (ADR 0020): the set-chip touch-history popover retired with the Tasks tab, and its `work.history` request is now rejected at the parse boundary | - |
| V5 | RETIRED (ADR 0020): task-first creation is now the rail's `drydock.rail.newTask` command plus the Task Hub composer; `Create & start chat` no longer exists as a Tasks-tab form | - |
| V6 | Chat: back ‹ button returns to Tasks; collapsed mounts details ("Auto: …" pre-start) expands to planned `/workspace/root-N ← host path` mappings | click, expand mounts, snapshot |
| V7 | Chat transcript for s-live: darker transcript background, full-width markdown blocks (h2, list, python code badge, mermaid diagram), user message as blue-accented card, raw HTML in a fixture message stays literal | select s-live, snapshot + inspect |
| V8 | Auth banner shows ONLY with Claude selected (needs-login fixture); hidden for Codex | switch provider select twice |
| V9 | Working set: glyphs, +N/−M colors, unreviewed dots, hover ✓/✕, ✕ arms to Confirm?, unrevertable row's ✕ disabled with tooltip; row click sends diff.openFile (harness log). Expanding Changes lives inside the single `.chat-scroll` body with the transcript/questions instead of shrinking those sections; `.composer` remains outside the scroll and pinned at the bottom. | expand Changes, inspect `.chat-scroll` + composer geometry, interact |
| V10 | Accept-all arms to `Accept 3 (M unreviewed)?`; opening a diff reduces M | click sequence |
| V11 | Inline access cards on s-live: ar-rw escalated (warning reads exactly `writes to your machine`; the typed confirm LABEL names the segment - `To approve, type ` + a monospace `.confirm-token` span reading `maya2026` for `D:\builds\maya2026` - with the GENERIC input placeholder `type it to confirm`; the confirm gates Allow; Deny one-click); ar-sens escalated despite read-only, its warning leads with the `sensitiveReason` line and ends `readable by the model` (`".env" matches a credentials/secrets file pattern - readable by the model`); typed mismatch keeps Confirm disabled; editing the path re-derives BOTH the expected segment and the `.confirm-token` text | type into confirm input, inspect disabled state + token text + warning line |
| V12 | PARTIALLY RETIRED (ADR 0020): the inline access cards in the chat still apply verbatim (V11) and now render in the chat rail. The Tasks-tab `.attention-summary` collapse/expand retired - the Task Hub's attention rail is its successor (no harness page yet) | check the inline card half on `index.html` |
| V13 | PARTIALLY RETIRED (ADR 0020): the demoted-row treatment lived on the Tasks tab. Attention now reads as the Tasks rail's native badge (host-side, out of harness scope) plus the fleet's pinned rows and coloured left edge (V59/V60) | - |
| V14 | `streamTurn("s-live")` while s-live selected: streaming text renders progressively, completed turn appends final markdown blocks, no duplicates | scenario, snapshot after 1s |
| V15 | Resume button on s-ended row (and ⋯ menu) → session flips active and selected in the chat rail. The `System → Chat diagnostics` half retired with the System tab (ADR 0020): the raw-stream tail (V61), the launch-command disclosure and the container terminal are the diagnostics now | click, verify |
| V16 | RETIRED (ADR 0012): the plan-docs pill and its panel are superseded by the Planner (`planner.html`, V52-V56); a `planDocs.*` request must be rejected at the parse boundary | - |
| V17 | RE-HOMED (ADR 0020, resolved 2026-08-02): the Tasks-tab Memory section's successor is Configure › Memories — quick-add, the approval gate and the browser are checked there (V78) over the kept `memory.*` contracts | - |
| V18 | Task Notes as its own chat-scroll section, not inside Changes: existing notes render above the note composer; the composer is visually distinct from the main chat ask box; note textarea is plain text, `Add note` appends a timestamped block, multiline/code-like indentation wraps without becoming review UI, and the `x` delete icon uses inline confirm | expand Task Notes, add multiline note, inspect order/style/delete |
| V19 | Session rename (chat header click) round-trips to fixtures. The task-card note edit half retired with the Tasks tab (ADR 0020) - task notes still render in the chat scroll (V18) | edit, verify |
| V20 | Delete chat: ⋯ → inline confirm → row disappears, panel resets to New chat | click sequence |
| V21 | Drag-drop simulation: dispatch drop with a file URI under demo-project → composer gains a `[file:/workspace/root-1/…]` token; sent user message renders it as `[filename]` with a tooltip containing container + host paths. Outside mounts → `[file-unmounted:…]` token + "not mounted" note | preview_eval synthetic DragEvent |
| V22 | 300px sanity: stage at narrow width, no horizontal scroll anywhere in the chat rail | resize stage via eval, snapshot |
| V23 | Elsewhere row (s-elsewhere): status dot is a hollow ring, meta shows "running elsewhere", no delete button, no Resume; clicking it opens the transcript read-only in Chat → composer + Send disabled with placeholder "This chat is running in another VS Code window (read-only here)"; ⋯ menu lacks Restart/End/Delete (only New chat + the read-only note) | select s-elsewhere, inspect dot/meta/composer, open ⋯ |
| V24 | RETIRED (ADR 0020): the Workspaces section moved to the left rail's Workspaces view (`drydock.workspaces`), which keeps the policy line, the `Use in new chats` picker, set editing and the delete guard. No harness page yet - guided manual verification | - |
| V25 | Clone session (s-clone): selecting it switches the Changes section to clone mode - a dim caption line `Clone sync - changes move by pull/push, not accept/discard` (`.clone-caption`) sits above the action row; one `asset_api@main` repo header, two rows (`publish_hooks.py`, `exporters/alembic.py`), the conflicted row carries a ⚠ marker + a `conflict` chip (`.chip.chip-conflict`) + `.conflicted` class; header shows **Pull all into editor** / **Push local → VM** (not Accept/Discard all); Pull all logs the harness result message ("Pulled 2 file(s) into editor") and empties the list; a per-file ✕ arms to Confirm then discards that row (clone.discard) | select s-clone, expand Changes, inspect caption/rows/⚠/conflict chip, click Pull all + per-file discard |
| V26 | Clone affordances: with s-clone selected the mounts details reads "1 clone · no live mounts" and lists a `clone` chip row `asset_api@main` + the "no live mounts" note; the Tasks-tab linked s-clone row shows a small `clone` chip next to its title. The composer has NO mode control (the [Plan|Develop] switch is retired, ADR 0012 - the only segmented strip is the Chat/Agents lens) | expand mounts details, snapshot Tasks task row, inspect composer |
| V27 | Task Review panel (`taskReview.html`, task `t-1`): a collapsed **`Legend`** disclosure (`details.tr-legend`) sits at the top of the navigator; opening it shows ONE line `± modify · + add · − delete · → rename · • unreviewed · ◑ opened · ✓ reviewed · ⚠ conflict`. Below it, 2 project groups (`asset_api`, `farm_submit`) with file rows carrying change-kind glyphs, `+N/−M` stat colors, a 💬 badge on the commented row (`src/publish_hooks.py`), a `clone` chip + ⚠ marker (+ `.conflicted` class) on the clone row (`farm_submit:queue.py`), and unreviewed dots on baseline rows initially; the degraded-fetch note line renders under the navigator. The per-row comment button reads **`💬+`** (`.tr-file-add`, title "Add a comment on this file") - NO bare `+` comment buttons (de-overloads the add-change glyph). Clicking a baseline row logs `diff.openFile` (`__harness.log`) and flips the row's reviewed marker: a comment-free row (`alembic.py`) → **✓** (`.tr-reviewed`); a row that still has an open comment (`publish_hooks.py`, opened) → **◑** (`.tr-opened`, title "Opened - has open comments"), NOT ✓ and no longer blank. The `💬+` affordance files a comment via `review.addComment` and bumps that file's 💬 badge. The dock groups threads by file (`<repo>:<path>:line`) with a status select over the six statuses; the delegated fixture comment renders dim/italic; the dock meta names the owning session. The header submit button reads `Submit review → agents (1)`; clicking it flips the open dock comment to delegated and writes **`Sent 1 comment to Rename sweep.`** to the result line (the response's `sentSessions` names the session; singular "comment"), and the button drops to `No open comments` (disabled). `__harness.scenario.agentRevised()` (resolves `trc-2`, grows `publish_hooks.py` +18→+24, pushes the update) → after the debounce the `publish_hooks.py` row briefly gets `.tr-row-flash` and a dim `updated · just now` stamp (`.tr-updated-stamp.tr-stamp-visible`) appears next to ↻ then decays (~5s); by contrast a plain `__harness.scenario.taskReviewUpdated("t-1")` (no fixture mutation) refetches silently - no flash, no stamp - and a manual ↻ with no change is likewise silent (a no-op refetch never announces; the first render never announces) | open taskReview.html, toggle Legend, inspect rows/badges/markers + `💬+` button, open alembic.py (→✓) and publish_hooks.py (→◑), submit + read result line, run `agentRevised()` then a plain `taskReviewUpdated` + a bare ↻ |
| V28 | RETIRED (ADR 0020): the per-task Review button moved to the Task Hub and the board; `taskReview.open` relay is checked from `agents.html` and `taskBoard.html` instead | - |
| V30 | Empty-state orientation + no-rollback tag. New chat (no messages): the transcript shows `.chat-empty` with two centered lines - line 1 `Ask the isolated agent to start.` (`.chat-empty-lead`); line 2 dim (`.chat-empty-modes`) the EXACT copy `Plan = read-only · Develop = edits your files (read-write)`. No `No messages yet.` box. Separately, on s-live's Changes working set the unrevertable delete row (`legacy/exporter_v1.py`, `revertSupported:false`) shows a dim inline `no rollback` tag (`.no-rollback-tag`) next to the stats, whose `title` carries the size-cap reason (`file exceeded the blob cap`); the disabled ✕ stays | reset to New chat + inspect `.chat-empty` lines; expand Changes on s-live + inspect the delete row's `.no-rollback-tag` + title |
| V31 | Subagent groups: a fan-out fixture session streams `agent.spawn` (2 children, one nested under the first, one that later fails) + child-attributed lines. The transcript shows collapsed `details.agent-group` blocks at the spawn points: header `⑂ <label>` + status dot (`.agent-group-dot.status-running` pulsing), live-ticking counts, and last-activity text while collapsed; the failed child ends with a bold red `· failed` chip (`.agent-group-chip.chip-failed`), the completed one a quiet ✓. Expanding shows the prompt line (`asked: …`), the child's prose as markdown blocks, activity rows with an `output` disclosure (`details.agent-entry-detail`, capped pre), a NESTED `.agent-group` for the grandchild, and the `result: …` line. Child prose does NOT appear in the main assistant flow (no interleaving); System → Chat diagnostics receives every child line prefixed `[label]`. Groups + expansion survive re-render; replay (reselect the session) rebuilds identical groups | scenario streams fan-out lines, expand/collapse, inspect classes + diagnostics prefixes, reselect for replay parity |
| V32 | Agents lens: the transcript strip shows `[Chat \| Agents]` once a session is selected; the role selector is hidden for now. Toggling to Agents renders `.agents-lens-row`s - root row (provider/model, bold) then indented child rows with status dots, `⑂ label`, counts (+`N tok` when usage present), a `· failed` chip on the failed child; clicking a child row switches back to Chat with that group expanded and scrolled into view. With no fan-out the lens shows the honest empty state; a codex-exec session shows the lifecycle-tier note; the Tasks tab shows the child session with its role chip (`.session-chip.chip-role`) and the parent's ⑂ chip (`.session-chip.chip-subagents`) while children run (from `session.agentActivity`) | toggle lens, click node→jump, inspect chips |
| V33 | PARTIALLY RETIRED (ADR 0020): the paged `.attention-stack` card still renders inside the chat and now lives in the chat rail - question cards, access cards, the `‹ i/N ›` pager and resolution behaviour are unchanged. The Tasks-tab summary line that led it retired; the Task Hub's attention rail is its successor | drive the stack on `index.html` |
| V34 | Composer model controls: the pinned `.composer` is a single rounded command box with transparent textarea chrome. Its bottom-left Model/provider tree opens **up and to the right**, stays wholly inside the chat/composer bounds at narrow or short view sizes, and scrolls internally if its rows do not fit. Selecting GPT-5.6-Sol rebuilds Thinking effort from its catalog and exposes **Extra High** plus the synthesized **Ultra** mode. Changing Model on a live same-provider chat persists the picker and does **not** send `chat.restartBackend`; changing Provider sends one `chat.restartBackend` and preserves the transcript. | select s-live, open `.composer-model-button`, compare the `.popover` rect with `.chat-tab` + `.composer`, pick GPT-5.6-Sol and inspect `.thinking-select`, then change Provider and check `__harness.log` |
| V29 | RETIRED (ADR 0020): Tasks-tab section order no longer exists. The rail splits those sections across three views (Tasks / Recents / Workspaces) whose order is VS Code's, not ours | - |
| V35 | Column pill (task-board and subtasks): each task card's top row shows a `.column-pill` (inside `.column-pill-dropdown`, a `<details>`) instead of the old state select - `t-1` reads "In Progress" tinted `.column-tint-in-progress` (blue), `t-2` reads "Backlog" tinted `.column-tint-backlog` (neutral). Clicking the pill opens `.column-pill-menu` with one `.column-pill-group-header` per non-empty category (Backlog/Pending/In Progress/Done, in that order) and `.column-pill-option` buttons ordered by sortOrder within each group; the current column carries `.selected`. Picking a different column sends `board.moveCard` (`cardKind: "task"`), the menu closes, and the pill's text + tint update from the response's refreshed board | expand Tasks, click `.column-pill` on t-1 and t-2, inspect group headers/order/tint, pick a column, verify pill updates |
| V36 | Subtask checklist header + rows: `t-1`'s `.subtasks-header` reads `SUBTASKS · 1 of 3 done` (only `st-3`, in `col-review`/done, counts; `st-1` in-progress and `st-2` pending do not); `t-2`'s reads `SUBTASKS · 0 of 1 done`. Rows render in `.subtasks-list` ordered by `sortOrder`: each `.subtask-row` has a `.subtask-title` (textContent), a `.subtask-status-pill` tinted by its column's category (same `.column-tint-*` classes as V35), and - only where applicable - a `.subtask-lock` (`st-2`, title explains the blocked reason) and a `.subtask-auto-chip` reading "auto" (`st-2`, autoStart true) | expand Tasks, inspect `.subtasks-header` text on t-1/t-2, inspect row order + `.subtask-status-pill` tint + presence of `.subtask-lock`/`.subtask-auto-chip` on st-2 only |
| V37 | Add / edit / delete subtask flow: typing into the `.subtask-add-row` input and pressing Enter (or clicking its Add button) sends `subtask.create` with only `title`; the response's refreshed task (now with the new subtask appended, defaulted to the Backlog column, autoStart false) re-renders the list. Clicking a `.subtask-row`'s summary line expands `.subtask-editor` (title input, description textarea, prompt textarea with placeholder exactly `Prompt (leave empty - not startable)`, an `Auto-start when dependencies finish` checkbox, a column pill scoped to that subtask, and a delete button); clicking a second row collapses the first (one expanded row per task). Editing the title/description/prompt and blurring (or Enter, Escape-to-revert - mirroring task rename/note-edit exactly) sends `subtask.update` with only the changed field; toggling the checkbox sends `subtask.update` with `autoStart` immediately, no blur needed. The delete button (`🗑` → `Confirm`, same `inlineConfirmButton` as task delete) sends `subtask.delete` and the row disappears from the refreshed task | add a subtask via Enter, expand/edit a row (title, description, prompt, checkbox), expand a second row (first collapses), delete a subtask via the two-click confirm |
| V38 | Blocked lock glyph: `st-2` (`isBlocked: true`) shows `🔒` (`.subtask-lock`) next to its title with a tooltip explaining the block; `st-1`/`st-3`/`st-4` (all `isBlocked: false`) show no lock glyph | inspect t-1's subtask rows for presence/absence of `.subtask-lock` |
| V39 | Auto chip: `st-2` (`autoStart: true`) shows a small "auto" chip (`.subtask-auto-chip`) next to its title/lock; `st-1`/`st-3`/`st-4` (`autoStart: false`) show none. Toggling the "Auto-start when dependencies finish" checkbox in a row's expanded editor flips this live via `subtask.update` | inspect t-1's subtask rows for `.subtask-auto-chip` presence, toggle the checkbox on st-1's editor and confirm the chip appears after refresh |
| V40 | RETIRED (ADR 0020): the Tasks-section `Board` button and per-card `Open on board ▸` moved to the rail's view/title command (`drydock.taskBoard.open`) and the Task Hub. Board rendering itself is unchanged (V41) | - |
| V41 | Board render (`taskBoard.html`): toolbar shows `＋ New task`, the task filter (`All tasks` + `t-1`/`t-2`/`t-3` titles), `Hide finished older than` select at `1 day`, `Columns…`, ↻. The rail renders the 6 columns in sortOrder with a 3px category top accent (`.tb-col.cat-backlog/.cat-pending/.cat-in-progress/.cat-done`), header name + small uppercase category tag + count pill. Cards: task cards (`.tb-task-card`, `data-task-id`) show a bold title, `TASK` tag, workspace chip (`.tb-ws-chip` reads the RESOLVED set name `pipeline` on `t-1`/`t-3` - names come from `workspace.state`) and subtask progress (`t-1` → `1/3 done`); subtask cards (`.tb-subtask-card`, `data-subtask-id` + `data-task-id`) show a 3px left stripe class (`stripe-h0..7`, same hue for all of one parent's cards), a dim parent-title line, ⚡ prompt glyph on `st-1`/`st-5` only, `auto` chip + 🔒 + dashed amber border (`.blocked`) on `st-2` only. Within a column tasks render before subtask groups; a group's cards cluster under one `.tb-group` | open taskBoard.html, snapshot + inspect classes/attributes/chips |
| V42 | Drag move + fallback menu: dragging `st-1` from In Progress to ToDo (synthetic dragstart/dragover/drop with a DataTransfer) sends `board.moveCard subtask st-1 -> col-todo` (`__harness.log`) and the card re-renders in ToDo (optimistic, then host board adopted). The ⋯ button (`.tb-menu-button`) on any card opens the `.tb-card-menu` "Move to" list naming every OTHER column; clicking an item moves the card the same way; Esc or an outside click closes the menu. A move rejected by the host (temporarily monkey-patch the harness handler to error) reverts the card and surfaces `move failed: …` on `.tb-status` | synthetic dnd events + menu clicks, check `__harness.log` + `.tb-status` |
| V43 | Age filter + hidden counter: at the default `1 day`, `st-6` (doneAt ~2 days) is ABSENT from Finished and the column footer reads `1 hidden · Show` (`.tb-hidden-toggle`); clicking it reveals the card and the button flips to `Hide old finished`; switching the age select to `3 days` shows `st-6` with no footer; back to `1 day` re-hides it. `t-3` (doneAt ~45 min) and `st-3` (doneAt ~20 min) stay visible in Review throughout. The `{ ageDays, taskFilter }` pair persists via getState/setState (reload keeps the selection) | toggle footer + age select, reload, inspect |
| V44 | Column settings modal: `Columns…` opens `.tb-modal` with FOUR fixed category lanes (backlog/pending/in progress/done, left-accented). Each chip is an inline rename input with ↑/↓ (within-category reorder) and ✕→`Confirm?` delete. Add via each lane's `＋ Add column…` input. Deleting `Blocked` (pending) then Save sends `board.columns.update` with `deletedColumnIds: ["col-blocked"]`; `st-2` (was in Blocked) lands in ToDo (nearest pending column) on the refreshed board. Attempting to delete the LAST column of a category is refused in-modal (`Cannot delete the last …` on `.tb-modal-error`, client-side; the host enforces the same). Renames + reorders round-trip on Save (`__harness.log` shows the columns.update line) and the rail re-renders in the new order | open modal, rename/reorder/add/delete, Save, inspect rail + `__harness.log` |
| V45 | Review quick-move (manual gate): every card in Review (the FIRST done column: `t-3`, `st-3`) carries a ghost `Move to Finished ▸` button (`.tb-ghost-move`, tooltip says nothing moves there automatically); cards in other columns (including Finished) have none. Clicking it on `st-3` sends `board.moveCard subtask st-3 -> col-finished` and the card moves (doneAt restamped by the mock host) | inspect ghost button presence per column, click one, check `__harness.log` |
| V46 | Blocked/auto chips + task focus + empty state: `st-2`'s card shows BOTH the `auto` chip (`.tb-auto-chip`) and 🔒 (`.tb-lock-chip`) with the dashed amber border; no other card shows either. The task filter set to `t-3` hides every card of `t-1`/`t-2` (rail shows only `t-3`'s task card + subtasks); back to `All tasks` restores them. `＋ Add subtask` (`.tb-add-subtask-button`) renders once per (column, task) presence; entering a title in In Progress for `t-3` sends `subtask.create` then `board.moveCard` (backlog default → target column) and the new card appears in In Progress. With `__harness.fixtures.tasks` emptied and `boardChanged()` pushed, the board shows the `.tb-empty` hint + `＋ New task` button; creating a task from there sends `task.create` and re-renders the rail | inspect st-2 chips, switch task filter, add a subtask in a non-backlog column, empty fixtures + `boardChanged()`, create a task |
| V47 | Start actions (`taskBoard.html`): `st-5` (prompt, unblocked, In Progress) shows `▶ Start` (`.tb-start`); clicking sends `subtask.start st-5` (`__harness.log`) and gets `accepted: true`. `st-1` (`isRunning: true`) shows the pulsing `running` chip (`.tb-running-chip`) and NO start button. `st-5` also wears the `failed` chip (`.tb-failed-chip`, tooltip carries `lastFailureAt`). Giving `st-2` a prompt via `subtask.update` makes its blocked card show a DISABLED `▶ Start` plus `Force start…` (`.tb-force`) whose two-click confirm (`Confirm force start`, `.armed`) sends `subtask.start st-2 (force)` - automation never forces, only this button does. Task card `t-3` shows `▶ Start ready (1)` (`.tb-task-start`; `st-5` is its one ready subtask) with a two-click confirm sending `task.start t-3`; its metadata includes `clone · all 2`. `t-2` (its only subtask sits in Backlog) shows no task-start button | click start/force/task-start with confirms, inspect chips + `__harness.log` |
| V48 | Dependency drag + boundary grey-out: every subtask card carries `.tb-dot-in` / `.tb-dot-out`. Pointerdown on `st-1`'s out-dot then pointermove starts the link drag: the rail gains `.tb-linking`, every card that is NOT a `t-1` sibling subtask (all task cards, `st-4`, `st-5`, `st-6`) gains `.tb-dim` (greyed, dots hidden) while valid targets' input dots glow, and a dashed ghost path (`.tb-edge-ghost`) follows the pointer. Pointerup over `st-3`'s card sends `subtask.dependency.add st-1 -> st-3`; Esc or dropping on nothing/a dimmed card cancels silently. Dragging `st-2`'s out-dot onto `st-1` (reverse of the existing `st-1→st-2` edge) is refused by the mock host - `.tb-status` shows the cycle message and `st-1`'s card briefly shakes (`.tb-shake`) | synthetic pointer events; inspect `.tb-linking`/`.tb-dim`/ghost, check `__harness.log` + `.tb-status` |
| V49 | Edge overlay + connections mode: at the default `Connections: on hover`, hovering `st-2` draws the `st-1→st-2` edge (`path.tb-edge.stripe-hN` in the parent hue, arrowhead at the input dot); `Connections: all` draws every edge with both endpoints rendered (`st-6→st-5` appears once `st-6` is revealed via the age footer or a 3-day filter); `Connections: hidden` draws none; the mode persists via getState/setState. Clicking an edge selects it (`.selected`, thicker) and floats `✕ Remove dependency` (`.tb-edge-delete`) at its midpoint; clicking that sends `subtask.dependency.remove st-1 -> st-2` and `st-2`'s 🔒 clears on the refreshed board; Esc or an outside click deselects | hover/click edges across all three modes, check `__harness.log` + st-2's lock |
| V50 | Diff view frames (`s-live` chat): the Changes tray shows the `This Turn / Session / Full Session` segmented toggle (`.diff-view-toggle`, Session active by default); `.working-set-files` has NO min-height (a 1-2 row list is that short) and caps at 30vh with its own scrollbar. `This Turn` refetches with `view: "turn"` and lists only `exporters/alembic.py`; `Full Session` lists 4 rows including `config/defaults.toml` dimmed (`.working-set-row.accepted`) with an `accepted` chip (`.chip-accepted`) and BOTH ✓/✕ disabled; Accept-all's armed count excludes accepted rows. Accepting a file in any view removes it from This Turn AND Session and flips its Full Session row to accepted. Per-view empty states name the frame ("No files changed since your last message." / "No unaccepted changes this session." / "No changes this session."); the toggle hides for the clone session (`s-clone`) and the no-session workspace scope | switch views, accept a row, inspect `.diff-view-toggle`/`.accepted`, compare list height at 1 vs many rows |
| V51 | Summarize to clipboard: the chat header's ⧉ button (`.chat-summarize-button`, between ⓘ and ⋯) opens a two-row menu - `Copy chat log` / `Copy AI summary` - that stays inside the panel (`.chat-summarize-popover .popover` narrower minimum). No session selected → both rows disabled. On `s-live` both enabled: `Copy chat log` closes the menu, flashes ✓, and `window.__harnessClipboard` holds the log stand-in; `Copy AI summary` acks, the button shows `…` disabled, and ~1.2s later the `session.summaryReady` push lands → ✓ + clipboard summary (seed `window.__harnessSummarizeFail = true` first → `!` + failure logged to Chat diagnostics, button re-enabled). On `s-ended` the AI row alone is disabled with the "resume it first" tooltip | click through both flavors + failure seed, inspect `__harnessClipboard`, button glyph states, menu row disabled/tooltips |
| V52 | Planner landing (`planner.html`): plans pane lists `pl-1` (5 artifacts · 2 open ✎) with archived `pl-2` under an ARCHIVED label; the intake form shows the aspect chips from the registry (archived `rollout` hidden, custom `brand-review` present), chip toggles flip `.selected` with a ✓ prefix, `Manage aspects` opens the inline editor (seeded rows carry `plan/<id>/ · seeded`, archive/restore round-trips), context-root Add/✕ rows render `:ro` badges, and `Create plan →` is disabled until the brief is non-empty. Creating acks, shows the info notice, opens the plan view, and ~0.6s later the `planner.sessionReady` push lands | fill intake, toggle chips, open manager, create; watch `__harness.log` |
| V53 | Planner plan view (`pl-1`): header carries ← Plans, the title, aspect chips, the live session dot, `Send instructions (2)`, ⟳ Regenerate, ⇄. The outputs rail groups by aspect (SYSTEM ARCHITECTURE / UI / UX / TESTING & VERIFICATION) with kind glyphs ▤◇▣⧉, rev badges, open-✎ counts, and hover ✎ rename (Enter commits `planner.artifact.rename`, empty restores the collected title); the selected row wears the accent bar. Below the draggable splitter (`.pl-divider`, ratio persists via getState) the OUTLINE lists the active document's headings - clicking one scrolls its block into view with the flash class; non-document artifacts list their annotations instead | click rows/outline, drag splitter, rename; inspect classes |
| V54 | Planner document provider: the design-doc theme (kicker `SYSTEM ARCHITECTURE · REV 3`, tight h1, quiet rules); hovering any block reveals ✎ and clicking opens the instruction box labeled `INSTRUCTION · BLOCK:N`; Add sends `planner.annotation.add block:N`, the dock (`INSTRUCTIONS · N`) gains an `open` entry, and the header send button count rises. Dock actions: Park (wont-fix, dimmed), Remove, Resolve/Reopen on delegated entries (delegated shows `delegated · rev N`); `Send instructions (N)` flips every open entry to delegated (`planner.sendInstructions` in the log) and fires the coarse `planner.changed` refetch. `open file ↗` logs `planner.openArtifact` | annotate, park, send; inspect dock entries + `__harness.log` |
| V55 | Planner layout system: each rail's « chevron collapses it to a 36px strip (outputs strip shows the present kind glyphs + a rust dot while open ✎ exist; chat strip shows 💬), the strip click re-expands, ⇄ swaps the rails via flex order (`.pl-body.swapped`, outputs order 2 / chat order 0), and all of it persists via getState/setState. Under 900px (resize the browser viewport, not the stage) `.pl-root.pl-narrow` auto-collapses both rails without touching the persisted prefs; a strip click flies the rail out as `.rail-overlay` (absolute, shadowed) and an outside click closes it | collapse/expand/swap, resize viewport to ~780px, fly out + outside-click |
| V56 | Planner chat rail on the SHARED components (P3): opening `pl-1` replays `s-live`'s timeline through chat/transcriptModel - the same user card / `## Findings` markdown / code + mermaid figures as the Chat tab, with subagent groups in the compact default renderer and the author label `Planner · <model>`. `streamTurn("s-live")` shows the streaming row + working indicator mid-turn and the final text after; the composer Send lands `planner.sendTurn` in `__harness.log`, streams the user line back, and ~0.7s later the doc's revision bumps (tree `updated` flash + viewer kicker REV N). Stop is enabled only while a turn runs. The SAME fan-out/stream checks on `index.html` (V7/V31/V32 flows) must still pass - the extraction may not regress the Chat tab | drive both pages after one `npm run bundle`; inspect `.pl-chat-log .chat-message` classes + `__harness.log` |
| V57 | RETIRED (ADR 0020): the Plan tab is gone; the Planner panel (`planner.html`, V52-V56, V68) is the planning surface. `planner.archive` / `planner.sendTurn` / `planner.aspects.*` are kept in contracts and still handled by `plannerPanelProvider`, but no webview sends them today | - |
| V58 | PARTIALLY RETIRED (ADR 0020): the Plan-tab half (header chip, recent rows, create-mode `for task` row, the task card's `Plan` button) is gone. The `planner.html` half still applies verbatim: the intake `For task` select, `pl-1`'s landing-card task meta, the plan-view `.pl-task-chip`, and the `task=` suffix in `__harness.log` | drive `planner.html` only |

| V59 | Agents panel (`agents.html`, UX overhaul P5) - FLAT list + row anatomy: no task groups, no chip clusters, no hover cards. Recent-first rows, each exactly 2 lines / ~44px: a 9px status dot in the shared roll-up vocabulary (`dot-awaiting`/`failed`/`running`/`starting`/`idle`/`done`/`offline`), the bold session title, the grey owning task (`no task` when unlinked), and a right-hand mono elapsed - running rows tick the turn duration (`a11y reviewer` `20m 08s`), awaiting rows read `waiting Nm` from the oldest pending item, view-only `s-elsewhere` reads `-`, everything else `Nm ago`. Line 2 is ONE 12px mono dim ellipsised activity line from the host-derived `sessionLines`: `s-live` `? Should the alembic exporter keep legacy 1.x sidecar files?` (question verbatim, outranks its command), `a11y reviewer` `$ Read`, `s-resuming` `resuming - recreating the runtime and clones`, `s-elsewhere` `running in another window - view only`, `s-waiting` `no activity this turn yet`. Rows waiting on a person pin to the top with a 2px amber left edge, failed rows pin under them in red | open agents.html after `npm run bundle`; inspect `.row` classes + heights + `.row-line2` text |
| V60 | Agents panel - filters, fold, grouping: the toolbar is title · `All \| Needs you \| Active` seg · ghost `Group by task` (default OFF) · faint mono `Σ 412.0k tok` · `?`, and nothing else (no search box, no density seg). `Active` (the default) hides `s-ended`; `All` shows it below a thin `Earlier · 1` fold carrying its result line `3 files changed across 2 repos - not landed` (settled rows sink only once they are ten minutes cold); `Needs you` leaves only the pinned rows. `Group by task` re-renders the SAME rows under `Alembic publish support` / `no task` headers with per-task counts and no fold. Empty states read `Nothing is waiting on you.` and `No agents are working right now. Switch to All to see finished chats.` Both toolbar picks round-trip through getState/setState (the harness holds webview state in memory only, so a reload resets it - persistence across reloads is a VSIX check) | click the seg + toggle; inspect `.fold`, `.task-head`, `.empty` |
| V61 | Agents panel - expand, row actions, live folding: the chevron expands in place → a mono raw-stream well (last 12 captured lines; exactly ONE `chat.rawStream <id>` in `__harness.log`, never re-fetched while open), indented subagent children (after `scenario.fanOut("s-live")`: `scribe` + `lister` at `depth-0`, `counter` at `depth-1`, each with its own `$ cmd`/output line and elapsed), and the meta line `codex · gpt-5.5 · codex-app-server · 412.0k tok · 96 calls`. Hover or focus a row swaps the elapsed for `Open chat ⇥ · Stop · ⋯`: row/title click logs `agents.openSession`, a child row appends ` node=<id>`, Stop shows while a turn is live and arms two-click before `chat.cancelTurn`, and the ⋯ menu (`End session` / `Container terminal` / `Raw stream`) logs `chat.endSession`/`runtime.openTerminal`/`chat.rawStream` and closes on Escape or an outside click. `Land changes` renders only on landable, non-running rows (`s-failed`, `s-ended`, `s-clone`) and arms to `Confirm land` → `agents.landSession`, a `Pulled 4 files` row note, and the pill leaves on the refetch; failed rows also carry `Retry ↗` (opens the chat - no session-retry request reaches this panel yet). Updates are damped: a `session.agentActivity` push (or `scenario.agentsTick()`) re-derives the line within ~1s rather than instantly, `chat.turnCompleted status=failed` flips a row red + pins it + reads `the last turn failed`, `scenario.agentsChanged()` costs exactly one debounced refetch, and no update ever scrolls the list | expand rows, drive the scenarios, click the actions; inspect `__harness.log` |
| V62 | Task Board density (`taskBoard.html`, ADR 0013): boots at the `data-card-detail="minimal"` default - `body.detail-minimal`, toolbar `Detail: minimal` select, NO ⚡/auto/workspace/clone/💬 chips, `st-2` keeps its ONE 🔒 chip (one-chip rule: running → failed → blocked), action rows / ⋯ / colour picker / `＋ Add subtask` hidden until the card (or column) is hovered or focused (cards are tabbable), and every card carries a `.tb-hovercard` (workspace/clone/progress/dates rows) that reveals after ~0.5s hover and never takes pointer events. `Detail: full` → classic chips return, quiet `created · updated` dates lines render, hover cards gone; `Detail: standard` → classic chips, no dates, hover cards back. The pick persists across reload (getState) and beats the body default | switch levels via the select, hover/focus cards, reload; count chips per card |
| V63 | RETIRED (UX overhaul P5): the Agents panel has ONE row shape, so `ui.cardDetail` no longer applies there - no `Min/Std/Full` seg, no per-row hover cards, no `data-card-detail` on `agents.html`. Board density is unchanged (V62) | - |
| V64 | Chain changesets (`taskBoard.html`, ADR 0014): at Detail `full`, `st-2` (depends on st-1, stored upstream) wears the `⎘ seed: upstream` toggle and `st-5` (unset) wears `⎘ seed: local`; clicking flips it via `subtask.update seedMode` and the refetch re-renders the new label. `st-6` (reveal the age-hidden Finished cards first) wears the quiet `⎘` unlanded marker at `standard`+`full`, never at `minimal`. At `minimal` the hover cards carry the story instead: `seed local HEAD/+ upstream changesets` on dependents, `changeset ⎘ captured · not landed` on st-6 | switch levels, click the seed toggle on st-2, reveal Finished, hover st-2/st-6 |
| V65 | Task recipes (`taskBoard.html` + `agents.html`, ADRs 0007/0002/0013): the board toolbar gains `＋ Recipe…` → modal lists `Implement + verify` (seeded) and `VFX shot pipeline · repo` (overlay); selecting swaps the step preview (deps as `← <upstream title>`, markers `auto · ⎘ upstream · <model> · ⚡`); Create is disabled until a title is typed, then materializes the task + subtasks with the DAG live (downstream cards born 🔒 blocked) and logs `task.createFromRecipe`. At Detail `full`, `st-1` wears the purple `claude-fable-5` model chip (hover card carries a `model` row at lower levels). Agents panel: the flat list's toolbar carries the faint mono `Σ 412.0k tok` fleet total (one form, no per-level variants and no per-task header totals - V59-V61) | open the modal, switch recipes, create; check chips + `__harness.log` |
| V66 | Policy rails (`taskBoard.html`, ADRs 0015/0007): `st-7` wears the dashed `queued` chip (one-chip at minimal; at `standard` the chip replaces its Start affordance entirely) and `st-5` wears `parked` (the `failed` chip yields to it; hover card reads `parked (failed twice under automation)`); at `standard`+ its Start button reads `↻ Retry`. The t-1 task card ⋯ menu gains `FAQ & auto-answer…` → modal lists the two fixture entries, Add appends a row (`task.faq.add` logged), ✕ removes, and the `Auto-answer: on/off` toggle round-trips `task.update … autoAnswerFaq=` - the task hover card reads `FAQ: N · auto-answer on/off` | check chips at min/std, open the FAQ modal, add/remove/toggle; inspect `__harness.log` |
| V67 | Landing + verify (`agents.html` + `taskBoard.html`, ADRs 0014/0007/0004): the bulk Landing drawer is retired - landing is per row (V61): `s-failed`, `s-ended` and `s-clone` carry `Land changes`, priced by their result line (`3 files changed across 2 repos - not landed`), two-click to `agents.landSession`. Board: `st-3` (Review) wears the warn `verify` chip at EVERY level (waiting-on-you; hover row `verify human check required · unmet`); at `standard`+ a green `Verified ✓` action stamps via `subtask.update … verified=true` and clears chip + action. Task Review: a non-user comment author renders the purple `agent`/`guard` badge (host-authored only - no harness fixture yet) | expand the drawer, pull the disjoint row, mark st-3 verified; inspect `__harness.log` |
| V68 | Plan → board (`planner.html`, ADR 0012): the `pl-1` plan-view header gains `⇪ To board…` → overlay lists the Test Plan's three checkbox items verbatim (checked + unchecked alike) with the hint naming the owning task (`Alembic publish support`); unchecking updates the `Create N subtasks` label; Create closes the overlay, shows `Created N subtasks on the board (nothing started).`, and logs `planner.subtaskCandidates` + `planner.materializeSubtasks` (harness appends them as backlog subtasks on t-1 + fires board.changed). A plan with no owning task disables Create with the pick-a-task hint | open pl-1, click ⇪ To board…, uncheck one, Create; inspect `__harness.log` |
| V69 | Continuity (`agents.html`, ADRs 0013/0015): `s-resuming` (`status: "starting"`) renders the pulsing hollow `dot-starting`, the line `resuming - recreating the runtime and clones`, and stays visible under the default `Active` filter (a booting session must not vanish). Durable queue/park restore is host-side (subtask_holds + orchestrator.restore) - out of harness scope, unit-tested in subtaskOrchestrator.test.ts | find the row under Active; check the dot class + line |
| V71 | RETIRED (ADR 0020): the Tasks compact view retired with the tab. The left rail is compact by construction - clusters, folds and a deduped Recents list - and has no expanded/compact toggle | - |
| V72 | Code Review panel (`codeReview.html`, docs/design/code-review-panel.md): header seg `All uncommitted \| Task \| Session` (Task default), `Hide whitespace` checkbox, `Unified \| Split` seg, open-comment count, `Send N comments to agents` (relays `taskReview.submit`), ↻. Sidebar: per-project sections (`.cr-proj`, +A −D totals), compressed folder rows (`.cr-folder`), file rows (`.cr-file-row`: change glyph, stats or image/binary word, `💬 N` badge from open comments, ⚠ on the conflicted clone file, `large` tag) with click-to-jump + scroll-spy `.current`; footer totals. Cards render in order with lazy `codeReview.fileDiff` fetches: text hunks (`.cr-hunk-sep` + `.cr-row-context/add/del`), image before/after (`.cr-img-cell` + byte sizes), binary byte-delta row, `largeDiff` files (package-lock, vendor/generated_api) collapsed with the expand note, conflict banner on queue.py. `Hide whitespace` refetches - publish_hooks.py drops from 3 hunks to 2; `Split` renders `.cr-split-grid` pairs; scope `Session` hides the clone file, `All uncommitted` adds the untracked `notes/review_notes.md` + a worktree honesty note. **Selection commenting**: selecting diff rows shows `.cr-composer` on mouseup under the LAST selected row with one `.cr-anchor-chip` per range (`.cr-row-pending` highlights); a second selection in another file adds a chip and moves the box there; `Comment` sends `codeReview.addNote anchors=N` (`__harness.log`), hides the box, clears highlights, and the note's threads render inline under each anchored range (`.cr-thread`, per-anchor); `Dismiss` (or Esc, or a chip's ✕ down to zero) clears without sending. Threads: author badges (`agent`/`guard` purple), delegated dim, status select sends `review.setCommentStatus`. Scope/whitespace/split/viewed persist via getState | drive scopes + toggles, synthetic Range selection + mouseup, comment + dismiss flows, inspect `__harness.log` |
| V73 | PARTIALLY RETIRED (ADR 0020): the needs-attention summary and its `.inbox-list` rows were Tasks-tab-only; the Task Hub's attention rail is the successor (no harness page yet). The per-turn dots in the transcript and the patch-export affordance still apply in the chat rail | check the transcript halves on `index.html` |
| V74 | RE-HOMED (ADR 0020, resolved 2026-08-02): the `＋ Remember…` quick-add row and the detected-tag chips now live in Configure › Memories (V78; the scope pill became a select — workspace/global on quick-add, +source-task on pending cards). Scope/tag *rules* stay in the same section; the briefing-provenance half is host-side | - |
| V75 | PARTIALLY RETIRED (ADR 0020): the System tab's `MCP Servers` definition manager moved to Configure › MCP Servers, which today adds and toggles but cannot edit an existing server's command/args/env or delete one (`mcp.save`/`mcp.delete` kept in contracts, no UI - known gap). The task/workspace/chat `MCP (n)` tri-state popovers and the chat ⋯ `Context debug` entry are unchanged and now run in the chat rail | drive the popovers + Context debug on `index.html` |
| V74 | Attachments (composer + questions): with `s-live` selected, pasting an image file into the composer textarea (synthetic ClipboardEvent with a DataTransfer File) sends `chat.uploadAttachment s-live pasted-….png` (`__harness.log`) and adds a removable chip (`.composer-attachments`) whose send-time token is `[file:/workspace/attachments/<name>]`; the composer 📎 (`.composer-attach-button`) opens a hidden multi-file input that uploads the same way; failures (no session selected, host error) log to Chat diagnostics instead of dropping silently. Question cards: the free-text answer row gains 📎 (`.question-attach-button`) and paste support - an uploaded file appends `[file:/workspace/attachments/<name>]` to the answer input, rides `question.answer` as plain text. Host semantics (guided manual verification): bytes land in the LIVE container at `/workspace/attachments/` via a bounded runtime exec (base64 over stdin - no mount changes, no restart; remote runtimes receive them over their own transport); uploads are capped at 12 MB and refused with a "start or resume this chat first" error when the session is not live | paste + click 📎 in both surfaces, inspect chips/answer value + `__harness.log` |
| V75 | Question extras (ADR 0016, fixture `q-3` on s-live): the stacked card wears the `manual check` chip (`.question-kind-chip`), renders 2 resolved illustrations (`.question-image`, click toggles `.zoomed`) plus one honest `⚠ /workspace/renders/missing.png (could not be read from the sandbox)` note (`.question-image-missing`), 3 checkable steps (`.question-steps`, the second with its own step image), and a checked-by-default `Stamp the gated subtask Verified ✓` row (`.question-verify-row`, present because `subtaskId: st-3`). Checking all steps and answering with the recommended option logs `question.answer q-3: … (steps checked: 3/3)` AND `subtask.update st-3 verified=true` - the summary drops both the question and the `1 to verify` inbox item in one gesture. Unchecking the verify box answers without stamping. Host semantics (guided manual verification): images resolve at capture via a bounded `base64 <path>` runtime exec (≤512 KB encoded, absolute in-sandbox image paths only), stored in `agent_questions.extras_json`; the protocol briefing documents `images`/`kind: manual-check`/`steps`/`subtaskId` on the question fence | page to q-3, inspect chip/images/steps, check all + answer, verify both log lines + summary delta |
| V76 | Sandbox previews (ADR 0017, fixture `pv-s-live-8080` on s-live): selecting s-live fetches `preview.list s-live` (`__harness.log`) and shows the pinned `.preview-strip` above the sandbox stats - one `.preview-chip` (live dot, title `Publish settings mockup (qt-dark)`, `Open`, ↗, ✕). `Open` logs `preview.open pv-s-live-8080` (real host: Simple Browser + one-time untrusted-content notice; ↗ adds `external`), ✕ logs `preview.stop` and hides the strip. Tasks tab: once the panel knows the preview, the inbox summary gains `· 1 preview` and the expansion shows the `▶` `.inbox-preview` row (`preview · <session title>`), whose click sends `preview.open`. Host semantics (guided manual verification): per-request proxy exec into the session's container, theme pack push to `/workspace/.drydock-themes/`, `drydock.prototypeThemes` config merge | select s-live, inspect strip + click Open/✕, back to Tasks → expand inbox, click ▶ row; check `__harness.log` |
| V70 | Demo-mode HITL + HTML prototype (any page: `?` → Start guided tour → End tour keeps Demo data): the onboarding plan carries the `Tour callout prototype` HTML artifact - it renders in the sandboxed prototype frame and `scripts: off → on` flips the frame to `allow-scripts` through the demo `planner.setPrototypeScripts` handler; `demo-session-review` carries a multi-step manual-check question (agent-in-a-container narrative, three one-click results) plus matching step-by-step instructions as its final transcript message (all demo `agent.text` lines are `final: true` - no streaming cursor). Answering or dismissing a demo question synthesizes a `question.resolved` push so the attention summary and chat cards clear exactly as Live; `policy.resolveAccess` resolves the demo access request (Allow drops `1 request` after the workspace refetch) | end a tour on `index.html`, expand attention: answer the manual-check question (count drops), Allow the access request; on `planner.html` select the prototype and toggle scripts |
| V77 | Runtimes fold (`agents.html`, ADR 0020): below the fleet sits a collapsed `▸ RUNTIMES` summary (`.runtimes-summary`, `aria-expanded=false`) - closed on boot, and no `isolatedRun.listRuntimes` in `__harness.log` until it is opened. Clicking it flips to `▾ RUNTIMES · 1`, fires exactly ONE `isolatedRun.listRuntimes`, and renders `r-1` as name (mono) · green `running` · a ticking uptime. `Stop` is invisible at rest and appears on row hover/focus; the first click arms it to `Confirm stop`, the second sends `isolatedRun.stopRuntime r-1` and refetches. `Clean up stale` sends `runtime.reconcile` then refetches. Escape disarms. With the mock host erroring the listing, the fold shows the error line plus `Sign in to Docker Sandbox` (sends `runtime.sbxLogin`) | expand the fold, hover a row, arm+confirm Stop, click Clean up stale, inspect `__harness.log` |
| V78 | Configure › Memories (`configure.html`, ADRs 0019/0020): the section leads with data — quick-add (persistent `Remember…` input, This workspace/Global select, toggleable detected-tag `for:` chips `python/pip/maya`), then pending cards, then the approved browser, then tag-rule rows. Cast `m-1` pending → nav reads `Memories 1` + amber dot; the card wears the amber left edge, an editable textarea preloaded with the proposal, an `Applies to` select (source task / workspace / everywhere) and its own tag chips. Approve after editing content+scope+tags sends `memory.resolve edits scope=… tags=…` and the row surfaces in the browser with the EDITED content (count + dot clear); Reject arms to `Confirm reject` (3s disarm) and a rejected candidate never renders. Quick-add Enter sends `memory.add scope=… tags=…`, clears the input, flashes `remembered` (NOT "saved to settings" — memories are store-local). Approved rows: 2-line clamped content, `ws:/task:/global` scope chip, tag chips, `added by you`/`agent proposal` origin, `open ↗` (sends `memory.open`), × arming to `Delete?` (sends `memory.delete`). `__harness.scenario.memoryCandidate({…})` streams a live proposal into the open section (card + count + dot appear, no toast); search `pytest` surfaces the approved row under a Memories group | drive quick-add + approve/reject/delete/open, fire the scenario push, inspect `__harness.log` |

Out of harness scope (needs guided manual verification against a real VS Code
window): real toasts + activity-bar badge, the VS Code diff editor, actual
mounts/read-only enforcement, provider logins, real runtime lifecycle, the
planner's real collection/hydration (host filesystem + sqlite - unit-tested in
plannerAppService.test.ts) and its `vscode.open` artifact jump; the Agents
panel's real sidebar navigation (`panel.showSession` → Chat tab selection +
Agents lens) and cross-window running-elsewhere heartbeats.

## Notes

- Synthetic `.click()` on card wrappers does not reach inner handlers -
  dispatch bubbling `MouseEvent`s on the inner title/chip elements instead.
- `streamTurn` sequences must ride a high monotonic counter; panel dedupe on
  `lastSequence` is correct behavior, not a bug to work around.
- Background-tab timer throttling stretches both the panel's debounce/decay
  timers and `preview_eval` polling waits, so timing measurements taken across
  evals are unreliable - assert state transitions (in-page timestamped
  observers) rather than durations.
- The comment dock's narrow-stacking media query keys on viewport width,
  which the harness stage cannot shrink - dock stacking needs guided manual
  verification in a real window, same as the 420px/360px host-window checks.
- Real git sync (both conflict directions, the no-commit invariant), the
  native diff editor, Comments-API gutter threads, real submit→revision
  turns, and real `updateWorkspaceFolders`/window-reload behavior all need
  guided manual verification in a real VS Code window - none of this is
  observable from the browser harness.
