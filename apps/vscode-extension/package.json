{
  "name": "drydock",
  "displayName": "Drydock",
  "description": "Drydock — the engineering AI workbench: contained coding agents, blast-radius approvals, cross-project review.",
  "version": "0.12.5",
  "license": "MIT",
  "publisher": "local",
  "private": true,
  "type": "module",
  "main": "./dist/extension.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/minimalefforttech/drydock.git"
  },
  "scripts": {
    "bundle": "node ../../tools/bundle-extension.mjs",
    "package:vsix": "node ../../tools/package-vsix.mjs"
  },
  "engines": {
    "vscode": "^1.101.0"
  },
  "activationEvents": [
    "onCommand:drydock.isolatedRun.start",
    "onCommand:drydock.isolatedRun.appServerProbe",
    "onCommand:drydock.isolatedRun.stop",
    "onCommand:drydock.isolatedRun.status",
    "onCommand:drydock.security.exportEvidence",
    "onCommand:drydock.panel.open",
    "onView:drydock.controlPanel"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "drydock",
          "title": "Drydock",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "drydock": [
        {
          "type": "webview",
          "id": "drydock.controlPanel",
          "name": "Control Panel",
          "icon": "media/icon.svg",
          "contextualTitle": "Drydock"
        }
      ]
    },
    "commands": [
      {
        "command": "drydock.isolatedRun.start",
        "title": "Isolated Run: Run Isolated Agent Prompt"
      },
      {
        "command": "drydock.isolatedRun.appServerProbe",
        "title": "Isolated Run: Probe Codex App Server Transport"
      },
      {
        "command": "drydock.isolatedRun.stop",
        "title": "Isolated Run: Stop Active Runtime"
      },
      {
        "command": "drydock.isolatedRun.status",
        "title": "Isolated Run: Show Runtime Status"
      },
      {
        "command": "drydock.panel.open",
        "title": "Drydock: Open Control Panel"
      },
      {
        "command": "drydock.taskReview.open",
        "title": "Drydock: Open Task Review"
      },
      {
        "command": "drydock.codeReview.open",
        "title": "Drydock: Open Code Review"
      },
      {
        "command": "drydock.clone.applyPatch",
        "title": "Drydock: Apply Patch to Folder…"
      },
      {
        "command": "drydock.session.attachTerminal",
        "title": "Drydock: Attach Terminal to Sandbox (experimental)"
      },
      {
        "command": "drydock.taskBoard.open",
        "title": "Drydock: Open Task Board"
      },
      {
        "command": "drydock.planner.open",
        "title": "Drydock: Open Planner"
      },
      {
        "command": "drydock.agents.open",
        "title": "Drydock: Open Agents"
      },
      {
        "command": "drydock.security.exportEvidence",
        "title": "Drydock: Export Security Events"
      },
      {
        "command": "drydock.taskReview.addComment",
        "title": "Task Review: Add Review Comment"
      },
      {
        "command": "drydock.taskReview.resolveThread",
        "title": "Task Review: Resolve Review Comment"
      }
    ],
    "menus": {
      "comments/commentThread/context": [
        {
          "command": "drydock.taskReview.addComment",
          "group": "inline",
          "when": "commentController == drydock.taskReview"
        }
      ],
      "comments/commentThread/title": [
        {
          "command": "drydock.taskReview.resolveThread",
          "group": "navigation",
          "when": "commentController == drydock.taskReview && commentThread == drydock.taskReview"
        }
      ]
    },
    "configuration": {
      "title": "Drydock",
      "properties": {
        "drydock.stateRoot": {
          "type": "string",
          "scope": "machine",
          "default": "",
          "markdownDescription": "Root directory for product state (SQLite event store, disposable workspaces). Leave empty to use `~/.drydock`. Managed mode always uses the fixed local location. Changing this requires a window reload."
        },
        "drydock.deniedPaths": {
          "type": "array",
          "scope": "machine",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Absolute paths excluded from runtime mounts, diff snapshots, and access-request approval. A mount intersecting a denied path (in either direction) is refused. Changing this requires a window reload."
        },
        "drydock.disableDefaultDeniedPaths": {
          "type": "boolean",
          "scope": "machine",
          "default": false,
          "markdownDescription": "**Not recommended.** By default, sensitive home-directory config roots (`~/.ssh`, `~/.aws`, `~/.gnupg`, `~/.kube`, `~/.azure`, `~/.docker`) are merged into `#drydock.deniedPaths#` so the agent can never mount them, even via an approved access request. Enabling this removes that protection in personal mode; managed mode ignores this setting. Changing this requires a window reload."
        },
        "drydock.security.allowedProjectRoots": {
          "type": "array",
          "scope": "machine",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Optional absolute folders that AI may access. Empty means no personal allowlist. This can only narrow an administrator-managed Studio policy. Changing this requires a window reload."
        },
        "drydock.security.cloneOnly": {
          "type": "boolean",
          "scope": "machine",
          "default": false,
          "markdownDescription": "Always use private Git clones instead of mounting live project folders. An administrator-managed Studio policy can require this. Changing this requires a window reload."
        },
        "drydock.security.omitSensitiveFiles": {
          "type": "boolean",
          "scope": "machine",
          "default": false,
          "markdownDescription": "Keep common environment/credential paths (`.env*`, `secrets/`, key files, cloud config folders) out of AI clones. If one was ever tracked in reachable Git history, Drydock blocks the clone rather than pretending deletion is secure. Implies clone-only mode. Changing this requires a window reload."
        },
        "drydock.security.omittedRepoPaths": {
          "type": "array",
          "scope": "machine",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Exact repo-relative files or folders to keep out of AI clones (for example `config/local`); descendants are included and globs are intentionally unsupported. `.git` cannot be omitted. Tracked/history content blocks the clone. Implies clone-only mode. Changing this requires a window reload."
        },
        "drydock.security.networkedAiEnabled": {
          "type": "boolean",
          "scope": "machine",
          "default": true,
          "markdownDescription": "Allow new AI requests on this machine. Turning this off prevents new turns and runtime starts; existing runtimes should be cleaned up separately. When an administrator-managed policy exists, it must also explicitly allow this machine. Changing this requires a window reload."
        },
        "drydock.runtime.pathAdditions": {
          "type": "array",
          "scope": "machine",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Extra directories prepended to `PATH` when Drydock runs runtime tools (`sbx` and the agent CLIs). Drydock already adds the standard Docker Desktop / Docker Sandbox bin dirs. Managed mode ignores local PATH overrides. Changing this requires a window reload."
        },
        "drydock.runtime.env": {
          "type": "object",
          "scope": "machine",
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "markdownDescription": "Additional **non-secret** environment variables for Drydock runtime tools. VS Code stores these as ordinary settings; managed mode ignores local environment overrides. Changing this requires a window reload."
        },
        "drydock.runtime.copyEnv": {
          "type": "array",
          "scope": "machine",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Names of environment variables to explicitly forward from the extension host to runtime tools. Drydock already inherits the host environment; managed mode ignores this setting. Changing this requires a window reload."
        },
        "drydock.runtime.appServerInactivityTimeoutMs": {
          "type": "number",
          "scope": "machine",
          "default": 300000,
          "minimum": 0,
          "markdownDescription": "Quiet-turn detector for Codex app-server turns: if the app-server sends no activity for this many milliseconds, Drydock logs a stall diagnostic (the turn is **not** auto-failed). The chat also offers a \"Give it a poke?\" nudge after ~30s of silence to try to break a standoff gracefully. Raise this if your turns legitimately run long silent operations (big builds/test suites); set to `0` to disable the diagnostic. Changing this requires a window reload."
        },
        "drydock.agentIdleThresholdMinutes": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "maximum": 120,
          "markdownDescription": "Minutes without delegated-agent activity before Drydock labels that agent idle in the visibility UI."
        },
        "drydock.codeBlockWordWrap": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Wrap long lines inside chat transcript code blocks. Disable to keep horizontal scrolling."
        },
        "drydock.ui.cardDetail": {
          "type": "string",
          "enum": [
            "minimal",
            "standard",
            "full"
          ],
          "default": "minimal",
          "markdownDescription": "Default detail level for cards on the Task Board and Agents panels. `minimal` shows one state chip per card and moves the rest into the hover card; each panel's toolbar **Detail** control overrides this per panel. Applies when a panel opens."
        },
        "drydock.orchestrator.maxConcurrentRuns": {
          "type": "number",
          "scope": "machine",
          "default": 0,
          "minimum": 0,
          "maximum": 64,
          "markdownDescription": "Maximum agent runs in flight at once. Further starts wait in a visible queue (manual starts jump to the front). `0` = **auto** - derived from this machine's cores and RAM (clamped 1-8)."
        },
        "drydock.autoAnswer.questions": {
          "type": "boolean",
          "scope": "machine",
          "default": true,
          "markdownDescription": "Master switch for task-FAQ auto-answering of agent questions. Each task ALSO has its own toggle (off by default), so nothing auto-answers until a task opts in. Every auto-answer lands in the transcript as a `[host]` receipt. Access requests are never auto-answered."
        },
        "drydock.mcp.configPath": {
          "type": "string",
          "scope": "machine",
          "default": "",
          "markdownDescription": "Host path of an `.mcp.json` (Claude Code project-scope format: `{\"mcpServers\": {…}}`). Entries are IMPORTED into the MCP registry (System tab) tagged *from settings* - editable only by editing this file. Servers run INSIDE the no-egress sandbox. Claude transports only in this iteration."
        },
        "drydock.memory.tagRules": {
          "type": "array",
          "scope": "machine",
          "default": [],
          "items": {
            "type": "object",
            "required": [
              "globs",
              "tag"
            ],
            "properties": {
              "globs": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Basename globs, e.g. \"package.py\" or \"*.usd\"."
              },
              "tag": {
                "type": "string",
                "description": "Tag applied when any glob matches, e.g. \"rez\"."
              }
            }
          },
          "markdownDescription": "Extra glob→tag rules EXTENDING the built-in table (`package.py`→rez, `pyproject.toml`→pip, `*.py`→python, `*.h`/`*.hh`→cpp, `*.ma`→maya, …). Tags are detected per mounted root at mount time (fast top-level scan) and select which tagged team memories a session briefing carries. Example: `[{ \"globs\": [\"*.usd\", \"*.usda\"], \"tag\": \"usd\" }]`."
        },
        "drydock.teamInstructionsPath": {
          "type": "string",
          "scope": "machine",
          "default": "",
          "markdownDescription": "Host path of a markdown file with studio-level standing instructions, appended to every session's host briefing (capped at 8 KB). Repo-level `CLAUDE.md`/`AGENTS.md` files are detected automatically and pointed at in the briefing."
        },
        "drydock.prototypeThemes": {
          "type": "array",
          "scope": "machine",
          "default": [],
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Theme file stem (a-z0-9-); lands as /workspace/.drydock-themes/<name>.css"
              },
              "cssPath": {
                "type": "string",
                "description": "Absolute host path of the CSS file."
              }
            },
            "required": [
              "name",
              "cssPath"
            ]
          },
          "markdownDescription": "Extra prototype theme stylesheets pushed into agent sandboxes alongside the built-ins (`qt-dark`, `slate-dark`, `vscode-dark`, `clean-light`). Agents link them from web prototypes to match a target application's look (ADR 0017)."
        }
      }
    }
  },
  "dependencies": {
    "@drydock/agent-adapters": "0.4.0",
    "@drydock/artifacts": "0.4.0",
    "@drydock/contracts": "0.4.0",
    "@drydock/core": "0.4.0",
    "@drydock/runtime-adapters": "0.4.0",
    "@drydock/storage-sqlite": "0.4.0",
    "@drydock/work-management": "0.4.0"
  }
}
